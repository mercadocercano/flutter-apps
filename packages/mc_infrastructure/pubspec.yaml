name: mc_infrastructure
description: Adaptadores de infraestructura - HTTP (Dio), storage, auth, sync engine.
version: 0.1.0
publish_to: 'none'

resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  mc_domain:
    path: ../mc_domain
  mc_application:
    path: ../mc_application
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
