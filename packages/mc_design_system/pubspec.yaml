name: mc_design_system
description: Design system de Mercado Cercano - tokens, theme, widgets compartidos.
version: 0.1.0

resolution: workspace

environment:
  sdk: ^3.11.5
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
