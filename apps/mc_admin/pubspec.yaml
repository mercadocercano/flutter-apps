name: mc_admin
description: MC Admin Flutter — panel de administración del marketplace para comercios.
publish_to: 'none'
version: 0.1.0+1

resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  mc_domain:
    path: ../../packages/mc_domain
  mc_application:
    path: ../../packages/mc_application
  mc_infrastructure:
    path: ../../packages/mc_infrastructure
  mc_design_system:
    path: ../../packages/mc_design_system
  flutter_bloc: ^9.1.0
  dio: ^5.7.0
  go_router: ^13.0.0
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  bloc_test: ^10.0.0
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
